# Skill 规则清单｜v0.0.0.8

第八个治理文件。把本机 Skill `study-project-orchestrator` 的成熟规则与流程**仓库化**，让它跟着 GitHub 一起迭代，而不是只活在本机 `~/.codex/skills/` 里。

**效力：** 本文件是**可勾选的执行清单**，不是新的权力来源。与前七个治理文件冲突时，**一律以治理文件为准**（冲突已在第二节逐条裁决）。规则编号 `S01`–`S43` 对应本机 Skill 的原始硬规则序号，方便双向追溯。

**来源：** 本机 `study-project-orchestrator`（`SKILL.md` + `references/project-files.md` + `agents/openai.yaml`），融合日期 2026-07-22。

## 一、本文件与治理文件的分工

| 层 | 谁回答 | 例子 |
|---|---|---|
| **要不要做、算不算数** | 前七个治理文件 | 什么算独立掌握、项目状态怎么改、什么时候暂停 |
| **具体怎么做** | 本文件 | 项目目录长什么样、每日流程几步、零基础认知链十环 |

治理文件管**判断与授权**，本清单管**手法与模板**。

## 二、冲突裁决（本机 Skill ⇄ 治理文件）

融合时发现 8 处冲突，`v0.0.0.4` 新增 1 处，`v0.0.0.5`、`v0.0.0.6`、`v0.0.0.7` 各新增 1 处，共 12 处，逐条裁决如下。**裁决结果即当前有效规则。**

| # | 本机 Skill 原文 | 治理文件 | 裁决 |
|---|---|---|---|
| C1 | 仓库路径 `LinzeColin/Notion/StudyProjects/<slug>/` | 仓库已更名 | **改为** `LinzeColin/NotionStudyProject/StudyProjects/<slug>/`。旧路径只在历史文档中保留 |
| C2 | `PROJECT_INDEX.md` 是路由事实源 | 派生索引 `_system/STUDY_INDEX.md` 才是 | **改为**派生索引路由；`PROJECT_INDEX.md` 降级为历史证据来源，不再路由 |
| C3 | `S18`：18:00 前无学习记录就标记「今日缺勤」并触发催促 | `AGENTS.md` 7.1：**「用户不需要每天学习；某天没有学习不算失败」** | **治理文件优先。** 可以**记录**「今天没有学习记录」这一事实，但：不判失败、不扣分、不自动暂停项目、不主动催促。只有 Owner 明确要求提醒时才提醒 |
| C4 | `S19`：7 天冲刺是默认 | 无固定课程天数，不得把天数当交付门 | **降级为建议默认**：可以推荐 7 天，但 Owner 未确认前不锁定，且**任何天数都不构成验收条件** |
| C5 | `S38`：外部复核至少 5 次尝试才算完成 | 未提及；但「未知写未知」是硬规则 | **保留该纪律**，并明确：外部复核**不是学习验收的前置条件**；复核方不可达时记为「阻塞不可用」+ 原因，**不得声称已完成** |
| C6 | 原 `Core Model` 一节：「Codex 是每日教练」 | Agent 中立，任何可实时读仓库的大模型都能当教学方 | **改为** Agent 中立表述；Codex / Claude / ChatGPT 都只是教学方的一种实现 |
| C7 | `S37`：每日课程结束必须同步 Notion | 「普通教学回复期间不改仓库、不同步 Notion」 | **协调**：只有 Owner 说了保存类指令时才同步。同步范围含 Notion 时间线数据库登记（`AGENTS.md` 第五节） |
| C8 | `S06`：新建 Notion 工作区前备份 | 未覆盖 | **保留**，写入下方 `S06`，作为不可跳过的前置步骤 |
| C11 | 协议代号裸写（`Route=J`、`E2`、`H1`） | Owner 反馈「这样写我看不懂」 | **改为中英双语**：字段名写「路由 Route」，取值写「`J`＝判断、探究与综合」。适用于**整条回复**，不只是状态块。标准译法唯一真源是 `STUDY_ARCHITECTURE.md` 11.7 |
| C12 | 深度八项**可以被八句话满足** —— 清单只规定「要写哪八项」，没规定「每项要写到什么程度」 | Owner **第三次**反馈「太浅太单薄」；前两次只改措辞，均无效 | **给八项各自加最小合格标准**（唯一真源：`TEACHING_METHOD_REGISTRY.md` 文末），低于线即未交付；**删掉两个逃生舱** —— 「不按字数机械衡量」删掉了唯一可度量的下限却没换上能失败的东西，「本次时间极短」是**模型自己就能判定**的条件，等于把钥匙交给被约束的一方；改为**交付前逐项静默自检**，不达标不许输出；**不设课程档位**，默认永远最深，唯一压缩条件是 Owner 亲口说「太长」且只压当轮 |
| C10 | 旧模板把状态块套进三反引号代码围栏当纯文本输出 | Owner 要求「要 Markdown 格式，不要纯文本」 | **改为渲染后的 Markdown**。代码围栏里的内容不会被渲染，字段名不加粗、表格不成表，Owner 看到的只是一坨等宽字 —— 这正是本次反馈的成因 |
| C9 | 旧根合同：「不默认输出超长讲义」 | `v0.0.0.4` 新增 8.3：每单元至少八项深度 | **两条直接打架，已改写。** 旧句删除，改为「每轮最多一个主要问题 —— **约束问题数量，不约束内容篇幅**」。Owner 明确反馈教学「太单薄、太浅显」，因此默认取深不取短；只有 Owner 说「太长」或时间极短才压缩 |

## 三、硬规则清单（可勾选）

标注说明：**【留】** 原样保留；**【改】** 按第二节裁决修改后保留；**【升】** 已升格进治理文件，此处只留指针。

### 3.1 语言与基本盘

- `S01` 【留】默认中文。
- `S02` 【留】每一步都为真实学习进展和投入产出优化，不为形式。
- `S03` 【改】项目路径统一为 `StudyProjects/<slug>/`（见 C1）。
- `S09` 【升】**绝不提交**密钥、令牌、Cookie、私人凭据或无关私密内容 → `MEMORY_RECOVERY_AND_EVOLUTION.md` 第十节。
- `S33` 【留】学习笔记、Notion 正文、每日记录、复盘默认中文；只保留必要的专业术语、接口名、分类代码、来源标题、工具名。

### 3.2 教学纪律

- `S10` 【留】**每次课程都先冷回忆，再教新内容。**
- `S11` 【留】每个重要概念都要讲清楚「Owner 该怎么用」。
- `S12` 【留】当行为、偏好、困惑或教学契合度暴露出有用证据时，更新个性化记录。
- `S20` 【留】**先学后建。** 新项目优先概念掌握、判断力和心智模型；实操和工程是后续的。
- `S21` 【留】工具搭建、自动化接线、脚手架**不算**能力进展，除非 Owner 已经能解释和判断底层概念。
- `S22` 【留】每次课程结束都给出后续几天的课程预告，并允许根据表现动态调整。
- `S41` 【留】滚动式分类学习必须**具体到某个分类**（例如「物理 / 量子物理 第3/30天」），不能只说属于哪个大组；一次教学只深讲一个分类，其他活跃分类只出现在紧凑的下一步队列表里。
- `S42` 【升】**零基础认知链**（详见第五节）→ `AGENTS.md` 第八节。
- `S43` 【留】多条活跃分类时默认**均衡追赶**：优先推进已验证进度最低的那条；进度相同则按配置顺序轮转。**未经冷回忆、验收、同步和状态更新的预览教学不算完成的一课。** 外部复核建议不能越过均衡推进，除非 Owner 确认。

### 3.3 项目生命周期

- `S13` 【留】**绝不混项目。** 存在多个项目时，读写前先确定当前项目短名。
- `S14` 【留】新项目在目标、领域、子方向、时长锁定或 Owner 接受默认之前，**不写项目目录**。
- `S15` 【留】Owner 给出宽泛领域时，先建设性发散：提出高价值的邻近行业、子方向、用例和前沿方向，再锁范围。
- `S16` 【留】提出范围扩展后，**必须等 Owner 明确授权**才能开始学习计划。
- `S17` 【升】确认总时长与第一天日期；每次课程报出 `第X/Y天`；**不创建 `D000` / 第 0 天** → `AGENTS.md` 7.1。
- `S18` 【改】**见 C3。** 可记录「今天无学习记录」，但不判失败、不自动暂停、不主动催促。
- `S19` 【改】**见 C4。** 7 天冲刺是建议默认，不是交付门。
- `S23` 【留】同时支持固定周期项目和滚动式项目。滚动式按周期、分组、分类、掌握百分比追踪，不假设单一线性结束日。
- `S24` 【升】多条并行轨道时维护机器可读状态表 → `_system/STUDY_INDEX.md` + 项目 `state.json`。
- `S30` 【留】滚动式分类项目**绝不**把一个大组当成单个 30 天单元。学习单元是**分类**；大组时长 = 分类数 × 每分类周期。
- `S31` 【留】开始滚动式多组计划前，先产出活跃组的明确路线顺序：分类代码、名称、计划窗口、以及为什么这个顺序投入产出高。
- `S36` 【留】排队中的组**不会自动从今天开始**；开始日必须由活跃槽位释放规则或明确项目状态推算。

### 3.4 Notion 与登记

- `S04` 【留】Notion 是笔记本；**不污染、不覆盖**原始笔记，除非 Owner 明确要求。
- `S05` 【留】新项目的 Notion 数据库或页面创建在 `Linz Dashboard` 下，与 `个人价值增长`、`制造业`、`Re:0`、`arXiv` 平级；其他工作区默认只读。
- `S06` 【留】**创建或导入任何新 Notion 工作区之前**，先把相关既有内容备份到 `NotionBackup/YYYYMMDD/`。
- `S07` 【改】默认在 Owner 说保存类指令后同步（见 C7）。
- `S08` 【留】默认同步内容是摘要、结构化学习行为和关键链接；有用且合适时可以同步完整正文。
- `S26` 【留】改动任何 Notion 页面或数据库前，**先取回并检查**现有属性、可见视图、命名风格，保持 Owner 的结构和格式。
- `S27` 【留】除非 Owner 明确要求那个具体对象、或范围已锁定并授权，**否则不新建** Notion 页面、数据库、属性、视图或项目记录。
- `S28` 【留】Owner 要求更新既有 Notion 对象时，**就地修改那个对象**，不建平行的伴生页面。
- `S29` 【留】时间线记录同时填写开始日和结束日；先读现有日期属性格式，再按同样格式写入。
- `S32` 【留】如果一行代表整个大组，其日期区间是大组路线窗口；活跃与排队状态在仓库状态文件里追踪，**不能只看 Notion 日期推断**。
- `S37` 【改】见 C7 + **`AGENTS.md` 第五节的双向登记规则**：任何在跑项目必须同时登记在 `README.md` 与 Notion 时间线数据库，两处一致；Notion 不可用时记录阻塞，**不得声称已同步**。
- `S40` 【留】Notion 页面图标必须匹配目标数据库的既有视觉体系：主色系、圆角、描边粗细、背景与阴影风格。`Codex Study Timeline` 默认蓝白科技风。优先复用 `_assets/notion-icons/` 下的既有素材（时间线页面用 `study-timeline-pages/`，分类页面用 `arxiv-taxonomy/`）。**不用随机表情符号或风格冲突的图标。** 读写图标失败时记录阻塞，不声称完成。

### 3.5 复核与自动化

- `S25` 【留】Owner 要求主动跟进或同步时，优先创建或更新**脱离聊天线程的定时任务**，让它直接读仓库与 Notion 状态。**没有真实调度器就不要声称有自动同步。**
- `S34` 【留】`study-project-daily-sync` 必须是脱离线程的本机定时任务，直接读状态，不依赖当前对话。
- `S35` 【留】有自动化时，同步是 Agent 的责任：读状态、更新过期同步记录、记录缺口，**不要让 Owner 当检查清单**。
- `S38` 【改】外部复核握手见第七节 + C5 裁决。
- `S39` 【留】敲定项目路线前，先做路线生成包，收集项目周期级、组级、分类级的路线建议、教学大纲、教学逻辑与内容偏重；原始尝试、阻塞和最终综合都存进仓库。

### 3.6 可见性与深度（`v0.0.0.4` Owner 授权新增）

- `S44` 【升】**启动核验默认静默** → `AGENTS.md` 6.1。教学前照常完成实时读取、项目解析、路由选择和状态加载，但**不向 Owner 展示** `STUDY_BOOTSTRAP`、仓库名、分支、提交号、已加载文件清单、项目盘点或内部路由推理。只有三种例外：Owner 要求审计、正在诊断解析或路由错误、无法实时读取仓库（此时必须显示 `GITHUB_NOT_VERIFIED`）。
- `S48` 【升】**协议代号一律中英双语** → `AGENTS.md` 第八节 + `STUDY_ARCHITECTURE.md` 11.7。不得裸写 `Route=J`、`E2`、`H1`、`active`；必须带中文含义。适用于整条回复。
- `S45` 【升】**可见回复以「今日课程状态」开头** → `AGENTS.md` 6.2，模板见 `SESSION_ACCEPTANCE_AND_REVIEW.md` 1.2。**必须是渲染后的 Markdown**（二级标题 + 加粗字段 + 引用块 + 五行表格），**不得**整块塞进代码围栏当纯文本。时区跟随 Owner 当前所在地，不硬编码。状态块之后直接进正课，不复述启动过程。
- `S46` 【升】**复习与新内容自动判定** → `SESSION_ACCEPTANCE_AND_REVIEW.md` 1.1。按距上次学习该项目的时间（同日 / 1–3 天 / 4–14 天 / 超过 14 天）自动决定探针数量，**不要问 Owner**；上一课有未结误解、最高只到 `E1`、或有到期复习与过期知识时一律优先复测。判定结果写进 `State=<上一状态> → <本轮状态>`。
- `S47` 【升】**默认教学深度八项 + 每项的最小合格标准** → `AGENTS.md` 8.3，达标线唯一真源在 `TEACHING_METHOD_REGISTRY.md` 文末。八项：问题框架、第一性原理模型、必要推导、完整实例演算、对比案例、反例或边界、现实迁移与投入产出、独立验收标准。**每项都得是可检查的具体内容，不能是关于该内容的描述**（推导至少三个可检验步骤；演算中间量一个不省；反例要给出让结论失效的具体参数值）。**禁止「一段概述 + 一道题」。**
- `S50` 【升】**参考课骨架是密度基准** → `_system/templates/REFERENCE_LESSON_SKELETON.md`。四段八槽各配一段合格示范和一段不合格反例；**规则只说要写什么，骨架说要写到多密**。用法三条：照抄密度不照抄句子；换主题不换形状；里面的示例主题是中性载体，**不得当成教学内容**。槽位 7（现实迁移）按定义必须绑真实项目，骨架里只给形状不给内容。冲突时以 `TEACHING_METHOD_REGISTRY.md` 文末的达标线表为准。
- `S49` 【升】**四段骨架 + 交付前静默自检 + 无档位** → `AGENTS.md` 8.3、`SESSION_ACCEPTANCE_AND_REVIEW.md` 第二节。八项按四段组织（为什么要学这个 ／ 它是怎么来的 ／ 什么时候它不成立 ／ 你拿它干什么），每段带小标题，**一段都不能删**；输出前逐项核对最小合格标准，不达标不许输出、不许宣称本单元完成、不许推进下一单元，自检不外显；**本仓没有课程档位**，默认永远最深，模型不得以「时间紧」「内容简单」自行降深度。

## 四、项目标准目录

```text
StudyProjects/<slug>/
├── 00_PROJECT_BRIEF.md      目标、周期、目标产出、成功标准、假设
├── 01_STUDY_PLAN.md         启动计划、冲刺计划、可选延伸
├── 02_TOP1_STRATEGY.md      能力定义、高杠杆概念、暂缓清单、能力树
├── 03_WEEKLY_PLANS/W01.md   本周主题、每日任务、产出、评分
├── 04_DAILY_LOGS/YYYY-MM-DD.md  签到、回忆、教学、练习、同步、下一步
├── 05_REVIEWS/W01_REVIEW.md 每周表现、计划契合度、阻碍、下周调整
├── 06_PERSONALIZATION/      学习偏好、行为模式、教学调整
├── 07_NOTION/               Notion 布局、笔记索引、同步日志
├── 08_REMINDERS/            提醒计划与自动化记录
├── 09_FRONTIER/             时效性领域的来源日志与前沿更新
├── metrics.csv              每日数值追踪
├── state.json               机器可读当前状态
└── HANDOFF.md               当前可执行状态（治理文件规定的标准模板）
```

**已有项目不强制批量改名**（治理文件要求保留真实路径）。滚动式项目可加 `10_PROGRAM_STATE/`（活跃轨道、分类目录、进度日志）。

## 五、零基础认知链（`S42` 细则）

**任何领域、任何一天，先过这一关再上高级内容。** 不从高级术语、论文名、公式、工具名或工程任务开始。

```text
可观察问题 → 最小单元 → 表示方式 → 机制 → 系统流程
→ 约束与成本 → 失效模式 → 使用场景 → 高价值动作 → 如何验证
```

| 环节 | 要回答什么 |
|---|---|
| 可观察问题 | 这个知识解决现实中什么问题或什么决策？ |
| 最小单元 | 系统操作的最小对象是什么？ |
| 表示方式 | 这个对象怎么被编码、度量、分类或建模？ |
| 机制 | 什么过程改变了这个对象？ |
| 系统流程 | 各部件从输入到有用输出怎么串起来？ |
| 约束与成本 | 什么限制了质量、速度、成本、安全或可靠性？ |
| 失效模式 | 误解或误用会怎样把系统弄坏？ |
| 使用场景 | 哪些任务该用它，哪些不该用？ |
| 高价值动作 | 今天最小的高价值动作是什么？ |
| 如何验证 | 怎么知道学习者真的懂了？ |

**执行规则：** 先问或推断这一关 → 答不出就用大白话直接教基础 → 关口能用了才进高级内容 → 用一个产出证明理解（概念图、机制表、决策规则、小备忘、验证清单）→ 把薄弱环节记进个性化文件。

## 六、新建项目与每日流程

流程主干见 `STUDY_ORCHESTRATOR_ROUTE.md` 第三、四节。本节只补充 Skill 特有的锁范围模板。

### 锁范围默认值

| 维度 | 默认 | 选项 |
|---|---|---|
| 学习目标 | 用于真实决策与产出 | A 理解 / B 应用 / C 变现或职业杠杆 / D 专家判断 |
| 领域 | 从 Owner 表述推断 | A 技术 / B 金融 / C 商业 / D 生产力 / E 学术 / F 其他 |
| 子方向 | 推荐 3–5 个具体方向 | A 基础 / B 工具 / C 案例 / D 项目 / E 前沿 |
| 时长 | 建议先议 7 天（见 C4） | A 7 / B 14 / C 30 / D 60 / E 90 |
| 每日时间 | 45–60 分钟 | A 30 / B 45–60 / C 90+ |
| 每周产出 | 可见交付物 | A 笔记 / B 项目 / C 报告或备忘 |
| Notion 角色 | 干净笔记本 + 轻结构 | A 原始笔记 / B 轻结构 / C 完整看板 |

**一轮最多问三个紧凑问题，并给出可一句话接受的默认推荐。**

## 七、外部复核握手（`S38` + C5）

在每日记录、仓库同步、Notion 同步完成**之后**执行。目的：发现路线盲点、改进契合度、挑战投入产出、收集替代解释。

**默认名单：** 配置的 Agent 复核方、ChatGPT、Claude、Perplexity，外加至少两个外部或已配置的复核方 —— 通常六次尝试。

**纪律（不可放宽）：**

- 复核方不可达时，在日志里记 `阻塞不可用` + 原因，**不得静默跳过或编造反馈**；
- **少于五次可追溯的尝试，不得声称握手完成**；
- 只有真实用过连接器、接口、浏览器会话、已配置 Agent 或 Owner 提供的输出，才算该复核方完成；
- 不把密钥、私人凭据或无关私密笔记发给外部复核方；
- 外部反馈是**建议性的**，不能越过 Owner 锁定的范围、安全边界或明确偏好；
- **外部复核不是学习验收的前置条件**（见 C5）—— 复核不可用不阻塞学习本身。

存储位置：`StudyProjects/<slug>/05_REVIEWS/external_ai_reviews/`，含复核包、复核结果、`external_review_log.csv`。

## 八、每周校准

每 7 天或按需：读最近的每日记录、`metrics.csv`、本周计划和个性化文件 → 就一致性、总时长、冷回忆、概念深度、实际产出、投入产出、动机、计划契合度打分 → 找出强信号、弱信号、阻碍和最有效的教学模式 → 写周复盘 → 更新下周计划、个性化和状态。

## 九、输出风格

**多用：** 表格、编号选项、默认推荐、紧凑状态表、明确的下一步、基于证据的调整。

**少用：** 长篇开放式问题、默认就上重型 Notion 体系、默认长篇讲义、泛泛的励志话术、未经核实的时效性断言。

每次有意义的运行结束时，给出：进度百分比、已完成、未完成、预计剩余、下一步建议。

## 十、本文件怎么迭代

1. 本机 Skill 有新规则 → 先写进本文件，再考虑是否升格进治理文件；
2. 与治理文件冲突 → 在第二节新增一行裁决，**不得静默覆盖治理文件**；
3. 规则升格进治理文件后 → 本文件对应条目改成 **【升】** + 指针，不重复正文；
4. 每次改动都要跑 `bash _system/validation/conformance_check.sh` 并更新 `_system/MAINTAINER_HANDOFF.md`。
