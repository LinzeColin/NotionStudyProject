# 执行与验收｜v0.0.0.5

对应 Governance 双平面标准的 `05_执行与验收`。回答：**这次做什么、怎么算做完、实际做了什么。**

等级定义（`E0`–`E4`、`H0`–`H4`、`R0`–`R6`、有效性状态）统一见 `STUDY_ARCHITECTURE.md` 第十一节口径字典，本文件不重复定义，只规定**怎么用**。

## 一、开始一次学习

Owner 可以只说：

```text
开始学习：<主题>
```

系统动作：

1. 实时读取治理文件、派生索引、当前项目的简介与交接；
2. 确定**一个**主合同（M / B / J）；
3. 定义**一个**可执行的验收标准；
4. 选择最多三个方法模块；
5. **按第 1.1 节自动判定该复习还是直接进新内容**，并选出 0–3 个复习探针；
6. 输出「今日课程状态」块（格式见 `AGENTS.md` 6.2），然后**立即开始正课**。

启动核验默认静默 —— 不显示回执、提交号、已加载文件清单（例外见 `AGENTS.md` 6.1）。

### 1.1 复习还是新内容：自动判定

**不问 Owner，自己根据上次学习时间和上次留下的证据判定。** 依据来源：项目 `HANDOFF.md` 的三队列与最高证据等级、`LEARNING_LOG.md` 最近相关记录、`state.json` 的最后学习日期。

| 距上次学习该项目 | 默认动作 | 探针数 |
|---|---|---|
| 同一天 | 直接进新内容 | 0–1（仅作衔接） |
| 1–3 天 | 先冷回忆上一课核心，再进新内容 | 1 |
| 4–14 天 | 冷回忆上一课核心 + 一个未结误区，通过才进新内容 | 2 |
| 超过 14 天 | 先复测上一课的验收标准；没过就重教该点，**不强行推进** | 2–3 |

**无论间隔多久，下列情况一律优先复测：**

- 上一课留有 `open` 状态的误解；
- 上一课最高只到 `E1`（提示后完成）；
- 有到期的回忆队列项，或有效性状态为 `recheck_due`。

**但是：** Owner 当前明确的目标优先于到期复习；探针最多 3 个、约占两成时间；**复习不得绑架会话**。判定结果写进状态块的 `State=<上一状态> → <本轮状态>`，让 Owner 一眼看到为什么是复习或为什么直接开新课。

### 1.2 「今日课程状态」块的固定格式

**必须输出渲染后的 Markdown，不是纯文本。** 把整块套进三反引号代码围栏是错的 —— 那样字段名不会加粗、表格不会成表，Owner 看到的只是一坨等宽字。

模板（原样使用这个结构）：

```markdown
## 今日课程状态

**日期** `<YYYY-MM-DD>`，`<时区>`
**课程导航** `<项目或分类>` / `<课程号>` / 第 `<N>`/`<M>` 天
**主题** <一句话主题>

> <项目状态一句话，含上一课已验收进度；首次开课就直说「此前无可验收证据，能力为未评估」>

| 项 | 值 |
|---|---|
| **Route** | `<M\|B\|J>` |
| **State** | `<上一状态>` → `<本轮状态>` |
| **Methods** | <方法一> ｜ <方法二> ｜ <方法三> |
| **Oracle** | <一句话验收标准> |
| **Review** | <探针内容>（<N> 个探针） |
```

规则：

- 时区**跟随 Owner 当前所在地**，不要硬编码；
- 项目状态那句用引用块，视觉上和路由表分开；
- `Route` / `State` 里的枚举值用行内代码，方便一眼扫到；
- 表格五行**一行都不能少**；本轮没有复习探针就写「无（0 个探针）」，不要省略该行；
- 状态块之后**空一行直接进正课**，不写「下面开始教学」这类过渡句。

## 二、教学循环

```text
框定 → 诊断 → 建模 → 练习 → 迁移/加压 → 更新
```

- 直接问题先回答，不要用诊断阻断；
- 每轮最多一个主要问题；
- Owner 说「不知道」时先给小提示，除非要求直接讲；
- 不连续机械追问；
- 根据时间、回答和负荷动态调整深度。

**默认深度是「深」不是「短」。** 每个新知识单元至少覆盖 `AGENTS.md` 8.3 的八项：问题框架、第一性原理模型、必要推导、完整实例演算、对比案例、反例或边界、现实迁移与投入产出、独立验收标准。

「每轮最多一个主要问题」约束的是**问题数量**，不是**内容篇幅** —— 一个核心问题要纵深讲透，**禁止用「一段概述 + 一道题」冒充完整教学**。只有 Owner 说「太长」或本次时间极短时才压缩。

## 三、提示等级怎么影响记分

按口径字典 11.2 的 `H0`–`H4` 记录**本轮最高提示等级**，并据此限制能记到的证据等级：

- `H0` / `H1` → 最多 `E2`（暂定独立）；
- `H2`–`H4` → 最多 `E1`（只算接触过）；
- 只有**延迟之后**的 `H0` 表现，或**陌生迁移**成功，才能升到 `E3`；
- 只有真实项目、考试、产物或已解析的判断，才能到 `E4`。

**同一轮纠正后答对，不能直接记 `E3`。**

## 四、三种主合同的验收标准

### M 的验收标准

必须同时满足：无材料、无关键提示；答案正确；能解释机制与原因；能说出边界、反例或易混概念；延迟之后或换个表述仍能提取。

### B 的验收标准

必须同时满足：真实产物可运行或可使用；**Owner 主导完成，而非 AI 代做**；改变输入、约束或环境后仍能处理；能诊断一条失败路径并解释怎么恢复；可复现，不是一次碰巧成功。

### J 的验收标准

必须同时满足：给出明确结论、选择或概率区间；事实、推断、假设、建议、未知分开；引用足够可靠且时效匹配的证据；提出最强的替代解释或反方；写出证伪条件、放弃条件或复查触发；结果可解析时安排结论复盘。

## 五、三个队列

Owner **不维护卡片、不打分**。模型根据获得帮助前的表现提出变更建议，维护方核验后写回。

### 回忆队列（用于原子知识）

按口径字典 11.4 的 `R0`–`R6` 调整：答错或空白 → `R0`；`H2`–`H4` 后完成 → 不升级或降一级；`H0`/`H1` 正确 → 升一级；延迟无提示 + 陌生迁移 → 可升两级；出现新反例 → 重开并降级；**内容过期 → 转有效性复查，不因记忆成功而通过**。

### 复做队列（用于技能）

触发条件四选一：下一次相关会话重做；改变输入或约束；增加异常路径；在真实产物中验收。

### 结论复盘队列（用于判断）

触发条件五选一：出现新证据；到了结果日期或事件；越过阈值；假设改变；来源过期。

## 六、复习预算

- **Owner 当前目标优先**；
- 每轮最多 1–3 个探针；
- 默认不超过约两成会话时间；
- 低价值、重复、无法独立评分的项目应退出队列；
- **不创建需要 Owner 维护的闪卡或按钮评分**。

## 七、知识时效

高波动内容必须记录：

```text
来源
权威级别
有效期起点
复查触发条件
被谁取代（如果有）
```

有效性状态见口径字典 11.5。**记忆时钟通过不代表有效性时钟通过** —— 内容过期时，「记得很牢」不构成掌握。

## 八、结束一次学习

触发词：`结束学习`、`结束学习并保存`、`保存学习记录`、`保存并同步 Notion`。

普通教学回复期间**不写仓库、不同步 Notion**。

### 无写权限（教学方）

只产出两样东西：

1. `STUDY_SESSION_DELTA_<UTC>.md`（模板见 `_system/templates/`）；
2. 一条通用维护提示词。

**不保存完整聊天，不声称已写入仓库。**

### 有写权限（维护方）

同一个模型也可能兼任。按 `AGENTS.md` 9.2 直接核验并写回：

1. 更新项目 `HANDOFF.md`；
2. 在 `_system/study-project-orchestrator/LEARNING_LOG.md` 顶部追加精简记录；
3. 更新 `README.md` 登记表、`_system/STUDY_INDEX.md`、`_system/MAINTAINER_HANDOFF.md`；
4. 同步 Notion 时间线数据库，或**记录阻塞原因**；
5. 跑验证、提交推送、返回提交号与回滚命令。

`保存并同步 Notion` 先完成仓库写入，再执行仓库中**既有**的 Notion 同步流程；本协议不新建 Notion 自动化。

### 会话增量必须包含

基线提交号、项目、主题、路由、验收标准；获得帮助前的关键回答摘要与置信度；最高提示等级；可靠 / 暂定 / 有辅助 / 未证明四类结论；误解、反例、迁移结果；三队列与有效性变化；下一个最小动作；来源时效；隐私与禁止持久化标记；允许与禁止改动的文件；治理规则修改建议（**默认不应用**）。

## 九、Owner 快捷控制

| 输入 | 行为 |
|---|---|
| 太长 | 只给结论、机制、下一步 |
| 太浅 | 加机制、推导、边界、反例、证伪 |
| 太抽象 | 改具体案例、数字或实际场景 |
| 先问我 | 冷回忆或诊断优先 |
| 直接讲 | 完整解释，停止机械追问 |
| 换一种讲法 | 类比、图解、第一性原理或对比 |
| 做陌生迁移 | 未讲过的新情境 |
| 加新视角 | 能改变判断或实验的新假设 |
| 加 ROI | 时间、成本、收入、效率、风险、机会 |
| 突破训练 | 超过当前基线但有恢复路径 |
| 切换项目 | 立即切换，不强制收尾 |
| 保存学习记录 | 按第八节保存精简记录 |
| 结束学习 / 结束学习并保存 | 按第八节结束并写入 |
| 保存并同步 Notion | 先写仓库，再执行既有 Notion 同步 |

本表与 `AGENTS.md` 9.1 是同一份约定；**冲突时以 `AGENTS.md` 为准**。

## 十、明确禁止的证据捷径

- 根据回复速度判断掌握；
- 根据语言流畅判断开放题正确；
- 看到答案后复述记为独立；
- 同一题反复做到熟记为迁移；
- 只换数字就称陌生迁移；
- 只有 AI 生成的可运行产物就记 `E4`；
- 高置信度结论没有来源、没有反方、没有复查触发；
- **在「无 AI 独立证据」区写入任何提示后完成或 AI 生成的内容**（触发证据纯净门失败）。

## 十一、本次交付怎么算做完

一次维护任务算完成，必须同时满足：

1. 受影响的项目 `HANDOFF.md` 已更新，且证据等级有据可查；
2. `README.md` 登记表与 Notion 时间线数据库一致（或已记录阻塞）；
3. `_system/STUDY_INDEX.md` 已按当前 HEAD 重建；
4. `_system/MAINTAINER_HANDOFF.md` 已更新，含下一个精确动作与回滚命令；
5. `bash _system/validation/conformance_check.sh` 通过（含四道门）；
6. 已提交并推送；
7. 仓库仍是单分支、0 未决合并请求、0 待办事项；
8. 本地无遗留分支与工作树。

**八条缺一条都不算完成。**
